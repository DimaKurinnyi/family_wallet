# Postgres. На Vercel используем pooled-строку Neon (в ней есть -pooler),
# иначе serverless-функции быстро выедают лимит соединений.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Секрет для подписи JWT. Сгенерировать: openssl rand -base64 32
JWT_SECRET=""
